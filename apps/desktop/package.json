{
  "name": "@personal-agent/desktop",
  "productName": "Personal Agent",
  "version": "0.1.2",
  "private": true,
  "description": "Windows desktop application for personal-agent",
  "author": "personal-agent",
  "license": "MIT",
  "type": "module",
  "main": "dist/main.mjs",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:production": "node scripts/build.mjs --production",
    "typecheck": "tsc --noEmit",
    "prepare:runtime": "pnpm exec turbo run build --filter=@personal-agent/web...",
    "start": "pnpm run prepare:runtime && pnpm run build && electron .",
    "package": "pnpm run prepare:runtime && pnpm run build:production && pnpm run forge:package:x64",
    "make": "pnpm run package && pnpm run make:installer",
    "make:installer": "electron-forge make --skip-package --platform=win32 --arch=x64",
    "make:arm64": "pnpm run prepare:runtime && pnpm run build:production && pnpm run forge:package:arm64 && pnpm run make:installer:arm64",
    "make:installer:arm64": "electron-forge make --skip-package --platform=win32 --arch=arm64",
    "forge:package:x64": "electron-forge package --platform=win32 --arch=x64",
    "forge:package:arm64": "electron-forge package --platform=win32 --arch=arm64",
    "lint": "echo lint ok",
    "test": "echo test ok",
    "clean": "node -e \"fs.rmSync('dist',{recursive:true,force:true});fs.rmSync('out',{recursive:true,force:true})\""
  },
  "devDependencies": {
    "@electron-forge/cli": "^7.11.2",
    "@electron-forge/maker-squirrel": "^7.11.2",
    "@personal-agent/web": "workspace:*",
    "@types/electron-squirrel-startup": "^1.0.2",
    "@types/node": "^22.20.1",
    "electron": "^43.2.0",
    "electron-squirrel-startup": "^1.0.1",
    "esbuild": "~0.27.7",
    "typescript": "^5.9.3"
  }
}
